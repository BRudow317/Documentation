Use the GitHub source directly with this workflow:

Clone the official repo: git clone https://github.com/heroku/12factor.git
Access the Markdown source: The content is written in Markdown files in the lib/ directory
Import into your wiki:

Convert the Markdown files to your wiki format (most wikis natively support Markdown)
Or use Jekyll/Pandoc to convert to HTML for maximum control